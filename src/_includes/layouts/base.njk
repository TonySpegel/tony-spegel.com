<!doctype html>
<html lang="de">
  {% include "./head.njk" %}

  <body {% if templateClass %} class="{{ templateClass }}"{% endif %}>
    {% include 'layouts/theme-switch.njk' %}
    {% include 'layouts/header.njk' %}

    <div class="glass"></div>
    <div class="bubble"></div>
    <div class="bubble-2"></div>

    <div id="sentinel-element"></div>
    <div class="main-wrapper">
      <main>
        {% if page.url == '/' %}
          <div class="intro">
            <h1>Hi, ich bin <span title="er/ihm">Tony</span>.</h1>
            <p>
              Ich bin Web Entwickler und immer daran interessiert
              Neues zu probieren - wenn es sich ergibt, fließt das
              in diese Website und in den Blog.
              <br>
              <br>
              Die meiste Zeit arbeite ich mit JavaScript, TypeScript,
              Angular, Lit, CSS und der E-Commerce Plattform Shopify.
              <br>
              <br>
              Woran ich arbeite und mich ausprobiere, findet sich auch unter den folgenden Links:
            </p>

            <ul class="social-links">
              {% for socialEntry in socials %}
                {% if socialEntry.include %}
                <li>
                  <a
                    href="{{ socialEntry.link }}"
                    rel="noopener noreferrer"
                    target="_blank"
                    title="{{ socialEntry.linkTitle }}"
                  >
                    <img
                      src="{{ socialEntry.img }}"
                      alt="{{ socialEntry.imgAlt }}"
                      width="{{ socialEntry.dimensions.width }}"
                      height="{{ socialEntry.dimensions.height }}"
                    >
                  </a>
                </li>
                {% endif %}
              {% endfor %}
            </ul>
          </div>
          <hr>
        {% endif %}
        {{ content | safe }}
      </main>
    </div>

    {% include 'layouts/sw-registration.njk' %}
    {% include 'layouts/footer.njk' %}
  </body>
</html>
